pipeline{
    agent{
     label{
          label 'built in'
          customWorkspace '/mnt/myproject'
          }
         }
          stages {
            stage ('stage-1'){
                        steps{
                             sh "mkdir test"
                            }
                             }
            stage ('stage-1'){
                        steps{
                             sh "mkdir folder1"
                             }
                            }
                           }
          }
